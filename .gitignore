# Dependencies
node_modules/

# Build output
client/dist/

# Environment secrets — never commit these
server/.env
client/.env
client/.env.local

# Database (JSON files) — lives on the server, not in git
server/data/

# OS noise
.DS_Store
Thumbs.db

# LibreOffice / Office lock files
.~lock.*

# Logs
*.log
npm-debug.log*
